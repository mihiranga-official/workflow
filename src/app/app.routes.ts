import { Routes } from '@angular/router';
import { WorkflowPDFComponent } from './workflow-pdf/workflow-pdf.component';

export const routes: Routes = [
    {
        path:'',component:WorkflowPDFComponent
    }
];
